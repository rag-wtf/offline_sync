// SQLite initialization for native platforms
// In sqlite3 v3, the library is loaded automatically via build hooks
// No manual configuration needed
Future<void> initializeSqlite() async {
  // Nothing to do - sqlite3 v3 uses automatic build hooks
}
